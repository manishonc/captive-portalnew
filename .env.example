# Database (use localhost when app runs on host; use 'db' when app runs in Docker)
DATABASE_URL=postgresql://radius:your_password@localhost:5432/radius_db

# Shared secret used for NAS (routers); can be overridden per-NAS via API
RADIUS_SECRET=shared_secret_for_nas

# Optional: app port
PORT=3000
